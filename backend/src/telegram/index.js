const telegramService = require('./telegramService');
const telegramClient = require('./telegramClient');
const telegramTemplates = require('./telegramTemplates');

module.exports = {
  telegramService,
  telegramClient,
  telegramTemplates,
};
